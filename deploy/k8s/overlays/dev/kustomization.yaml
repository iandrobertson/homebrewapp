apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - path: replicas.yaml
# HPA, PodDisruptionBudget, and the image-patch CronJob are omitted on purpose.
# Observability can be installed separately from deploy/observability.
